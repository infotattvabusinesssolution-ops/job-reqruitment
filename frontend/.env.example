# Frontend Server Port
VITE_PORT=5173

# API Base URL (Relative path '/api' uses Vite dev server proxy; use absolute URL in production if needed)
VITE_API_URL=/api

# Backend Server Base URL (used for proxying /api, /uploads, and /socket.io in Vite)
VITE_BACKEND_URL=http://localhost:5000
